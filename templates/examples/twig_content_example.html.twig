{# Fixture example for the "twig_content" block kind (see BlockFixtureProvider) - shows what
   "templatePath" points at, nothing more #}
<p>Contenu Twig personnalisé, inclus depuis <code>templates/examples/twig_content_example.html.twig</code>.</p>
