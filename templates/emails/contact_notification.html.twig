{% extends "@c975LSite/emails/layout.html.twig" %}

{# Composed via the "contact_notification" EmailTemplate, referenced by the "contact" Form #}
{% block content %}
    {{ email_template_body('contact_notification', {
        form_name: form.name,
        fields: fields,
    }) }}
{% endblock %}
