{# Fragment (no layout) - renders a plain Blocks collection, used by
   PageController::resolveCollectionDetail() to render a collection's detail Page's own blocks with
   "collectionItem" (see CollectionItemContext) set to the current item's data. #}
<twig:c975LUi:Blocks:Blocks blocks="{{ blocks }}" />
