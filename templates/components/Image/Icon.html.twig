{# <twig:c975LSite:Image:Icon src="your-path/file.ext" [class="your-css-classes"] width="width" height="height"/> #}
{% set class = class | default('') %}
<img src="{{ asset(src) }}" alt="" class="icon {{ class }}" width="{{ width }}" height="{{ height }}" loading="lazy">