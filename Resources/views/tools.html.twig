{% trans_default_domain 'toolbar' %}

{% filter spaceless %}
{# Dashboard #}
{{ toolbar_button(path('site_dashboard'), 'dashboard', 'md', 'label.site'|trans) }}

{# Config #}
{% if type != 'config' %}
    {{ toolbar_button(path('site_config'), 'config') }}
{% endif %}

{# Signout #}
{{ toolbar_button(path('user_signout'), 'signout') }}

{# Other dashboards #}
{{ toolbar_dashboards('md') }}
{% endfilter %}
